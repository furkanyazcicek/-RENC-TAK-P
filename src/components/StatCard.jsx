/**
 * Geriye dönük uyumluluk katmanı.
 *
 * StatCard artık tasarım sisteminin bir parçası: `src/components/ui/StatCard.jsx`
 * (eski `accent="good|warn|bad"` değerleri desteklenmeye devam ediyor,
 * ek olarak `trend` ve `loading` props'ları eklendi).
 *
 * Yeni kod yazarken tercih edin:  import { StatCard } from './ui'
 */
export { default } from './ui/StatCard'
